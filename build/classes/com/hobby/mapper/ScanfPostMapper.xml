<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.hobby.mapper.ScanfPostMapper">
 
 	<insert id="replyPost" parameterType="com.hobby.pojo.Reply">
 		insert into reply (info,time,uid,pid,id)
 			values(#{info},#{time},#{uid.username},#{pid.id},#{id})
 	</insert>
 	
 </mapper>